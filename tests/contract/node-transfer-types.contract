# Contrato do artefato types: types/transfer/transfer.d.ts
# Derivado de ~/workspace/bank/sdk-node/types/transfer/transfer.d.ts

declaration declare module 'starkbank'
declaration export class Transfer
namespace export namespace transfer

# --- Paridade ainda nao alcancada: assinaturas tipadas e sub-recursos ---
todo signature export function get(id: string, params?:{ user?: Project | Organization | null}): Promise<Transfer>
todo signature export function pdf(id: string, params?:{ user?: Project | Organization | null}): Promise<Buffer>
todo signature function _delete(id: string, user?: Project | Organization): Promise<Transfer>
todo inner export class Rule
todo inner export class Log
todo namespace export namespace log
