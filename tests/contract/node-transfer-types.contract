# source:    _references/sdk-node/types/transfer/transfer.d.ts

declaration declare module 'starkbank'
declaration export class Transfer
namespace export namespace transfer

signature export function get(id: string, params?:{ user?: Project | Organization | null}): Promise<Transfer>
signature export function pdf(id: string, params?:{ user?: Project | Organization | null}): Promise<Buffer>
signature function _delete(id: string, user?: Project | Organization): Promise<Transfer>
inner export class Rule
inner export class Log
namespace export namespace log

signature export function page(params?
