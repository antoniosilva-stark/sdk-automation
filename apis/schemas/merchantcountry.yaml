# fonte: starkbank/sdk-python@9ef6834 · starkbank/merchantcountry/__merchantcountry.py
components:
  schemas:
    MerchantCountry:
      type: object
      description: "MerchantCountry's codes are used to define countries filters in CorporateRules."
      properties:
        code:
          type: string
          description: "country's code. ex: \"BRA\""
        name:
          type: string
          description: "country's name. ex: \"Brazil\""
        number:
          type: string
          description: "country's number. ex: \"076\""
        shortCode:
          type: string
          description: "country's short code. ex: \"BR\""
      required:
        - code
        - name
        - number
        - shortCode

    MerchantCountryCreate:
      type: object
      description: "MerchantCountry creation request"
      properties:
        code:
          type: string
          description: "country's code. ex: \"BRA\""
      required:
        - code
